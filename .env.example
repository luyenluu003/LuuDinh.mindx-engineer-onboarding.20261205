# KB API Configuration

# Client type: mock or http
KB_CLIENT_TYPE=mock

# KB API URL (used when KB_CLIENT_TYPE=http)
KB_API_URL=http://localhost:3000/api/kb

# API Key for authentication (optional)
KB_API_KEY=

# Request timeout in milliseconds
KB_TIMEOUT=30000
